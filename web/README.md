# Web Frontend Scaffold

This directory is the web-based frontend project scaffold.

## Current state

- Minimal static app shell to start building the board UI.
- Intended to consume an API provided by the backend engine service.

## Next recommended step

Replace this scaffold with your preferred framework (React + TypeScript is a good default) and wire it to API endpoints.
